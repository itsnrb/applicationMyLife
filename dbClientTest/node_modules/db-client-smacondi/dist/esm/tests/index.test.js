import { describe, it, expect } from 'vitest';
import * as indexExports from '../index.js';
import { getDbClientSmacondi, closeAllDbClients } from '../dbClientSmacondi.js';
import { DbClient } from '../dbClient.js';
describe('index.js', () => {
  it('should export getDbClientSmacondi function', () => {
    expect(indexExports.getDbClientSmacondi).toBe(getDbClientSmacondi);
  });
  it('should export closeAllDbClients function', () => {
    expect(indexExports.closeAllDbClients).toBe(closeAllDbClients);
  });
  it('should export DbClient class', () => {
    expect(indexExports.DbClient).toBe(DbClient);
  });
});